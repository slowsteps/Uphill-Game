package  {		import flash.display.MovieClip	import flash.events.Event	import flash.geom.Point		public class Flare extends MovieClip {		private var myMain:Main		private var spawn:MovieClip			public function Flare(inMain:Main) {			// constructor code			myMain = inMain			myMain.addChild(this)			visible = false			stop()		}		public function show(inSpawn:MovieClip) {			//spawn can be dino or carclip			spawn = inSpawn						var myPoint = new Point()			//myPoint.x = myMain.carclip.x			//myPoint.y = myMain.carclip.y			//myPoint = myMain.carclip.parent.localToGlobal(myPoint);						myPoint.x = spawn.x			myPoint.y = spawn.y			myPoint = spawn.parent.localToGlobal(myPoint);						x = myPoint.x			y = myPoint.y												addEventListener(Event.ENTER_FRAME,onEnterFrame)			play()			visible = true		}				public function onEnterFrame(e:Event) {			if (this.currentFrame == this.totalFrames) {							removeEventListener(Event.ENTER_FRAME,onEnterFrame)				visible = false			}			else {				//x = myMain.carclip.x				//y = myMain.carclip.y			}		}	}	}